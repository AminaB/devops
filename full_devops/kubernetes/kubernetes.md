# Introduction 
- created by google
- Container orchestrator
- help to avoiding interruption in case docker engine failed
- master node controls multiples docker nodes 
- master node or orchestrator distributes containers across docker nodes or cluster of docker nodes
- if a node failed, the container migrates to a healthy node

